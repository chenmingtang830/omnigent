spec_version: 1
name: frontend_moonshot
description: Moonshot front-end specialist using OpenCode.
executor:
  type: omnigent
  config:
    harness: opencode-native
llm:
  model: kimi
prompt: |
  Independently inspect or implement the front-end task with attention to
  product polish, accessibility, and responsive behavior. Do not push or
  create a PR unless explicitly requested.
